<div class="bg-card p-6">
  <p class="mb-4 text-xs font-bold tracking-widest text-foreground/40 uppercase">
    Prossimi eventi
  </p>

  <div class="flex flex-col">
    {% for row in rows %}
      {{- row.content -}}
    {% endfor %}
  </div>

  <a href="/events" class="mt-4 inline-flex items-center gap-1.5 text-xs font-semibold text-primary transition-all duration-200 hover:gap-3">
    Tutti gli eventi<svg
      class="h-3.5 w-3.5"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2.5"
      stroke-linecap="round"
      stroke-linejoin="round"
      aria-hidden="true"
    >
      <path d="M5 12h14M12 5l7 7-7 7" />
    </svg>
  </a>
</div>
